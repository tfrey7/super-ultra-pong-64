/*
 * Super Ultra Pong 64: Remastered -- the TV and HDTV overlays for eras 5 to 10.
 *
 * Empty on purpose: the follow-up card fills it in. It registers its overlay
 * kinds with PongDisplay.registerOverlay(kind, fn) and points the rows of the
 * eras that use them at those kinds (src/display.js has the contract). It is
 * already in index.html's script list, after src/display-crt.js.
 */
