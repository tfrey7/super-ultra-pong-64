/*
 * Era 4 -- 1991 Super Nintendo, the top of the ladder.
 *
 * PLACEHOLDER: until the Super Nintendo card lands this rung draws era 1's
 * look, which is what `like: 1` means. That card replaces this file and
 * touches no other.
 */
(function (root) {
  'use strict';
  root.PongRender.registerEra({
    era: 4,
    name: '1991 Super Nintendo',
    placeholder: true,
    like: 1
  });
})(typeof globalThis !== 'undefined' ? globalThis : this);
