/*
 * Era 2 -- 1985 NES.
 *
 * PLACEHOLDER: until the NES card lands this rung draws era 1's look, which
 * is what `like: 1` means. That card replaces this file and touches no other.
 */
(function (root) {
  'use strict';
  root.PongRender.registerEra({
    era: 2,
    name: '1985 NES',
    placeholder: true,
    like: 1
  });
})(typeof globalThis !== 'undefined' ? globalThis : this);
